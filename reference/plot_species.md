# Plot species

Function for visualizing the predictions from species_predicts

## Usage

``` r
plot_species(spat.raster = NULL, variable = NULL)
```

## Arguments

- spat.raster:

  SpatRaster object of the predictions.

- variable:

  Attribute within the SpatRaster object for plotting.
