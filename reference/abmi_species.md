# Provide users with species lookup table

This function provides the lookup table of all species models.

## Usage

``` r
abmi_species()
```

## Value

A dataframe with all of the species information include model region and
validation statistics.

## Examples

``` r
if (FALSE) { # \dontrun{
species.lookup <- abmi_climate()
} # }
```
